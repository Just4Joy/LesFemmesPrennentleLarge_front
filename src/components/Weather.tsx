import React from 'react';

const Weather = () => {
  return (
    <div className="weather">
      <button className="weather__button" type="button">
        session au large
      </button>
    </div>
  );
};

export default Weather;
